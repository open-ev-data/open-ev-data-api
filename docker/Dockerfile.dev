# Development Dockerfile - Compiles binaries inside the container
# This Dockerfile is suitable for local development and testing where
# pre-compiled binaries are not available. Build time is longer but
# requires no external build step.
#
# Usage: docker build -t ev-server -f docker/Dockerfile.dev .
# Or use docker-compose: docker-compose -f docker/docker-compose.yml up

FROM rust:1.92.0-slim-bookworm AS builder

WORKDIR /app

RUN apt-get update && apt-get install -y \
    pkg-config \
    libsqlite3-dev \
    curl \
    && rm -rf /var/lib/apt/lists/*

COPY Cargo.toml Cargo.lock ./
COPY crates ./crates

RUN cargo build --release -p ev-server

FROM debian:bookworm-slim

WORKDIR /app

RUN apt-get update && apt-get install -y \
    libsqlite3-0 \
    ca-certificates \
    curl \
    && rm -rf /var/lib/apt/lists/*

COPY --from=builder /app/target/release/ev-server /app/ev-server

RUN useradd -r -s /bin/false appuser && \
    chown -R appuser:appuser /app
USER appuser

ENV DATABASE_URL=/app/vehicles.db
ENV PORT=3000
ENV HOST=0.0.0.0
ENV LOG_LEVEL=info
ENV ENABLE_COMPRESSION=true
ENV ENABLE_OPENAPI=true

EXPOSE 3000

HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
    CMD curl -f http://localhost:3000/api/v1/health || exit 1

ENTRYPOINT ["/app/ev-server"]
